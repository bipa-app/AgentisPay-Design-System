{
  "name": "agentispay-design-system",
  "private": true,
  "version": "1.1.0",
  "description": "AgentisPay Design System — monorepo root",
  "scripts": {
    "build": "pnpm --filter './packages/*' build",
    "build:tokens": "pnpm --filter @bipa/tokens build",
    "build:icons": "pnpm --filter @bipa/icons build",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "packageManager": "pnpm@10.32.1",
  "devDependencies": {
    "@changesets/cli": "^2.30.0"
  }
}
